Parse toàn bộ các hàm trong class cần kiểm thử thành 1 danh sách ASTNode
Duyệt danh sách ASTNode để tìm hàm cần kiểm thử
CONCOLIC TEST REPORT
Hàm thực hiện:
public int sampleMethod(int a,int b,int c){
  if (a > 10) {
    if (c < 20) {
      for (int i=0; i < b; i=i + 1) {
        System.out.println("hello");
      }
      if (b < -5) {
        System.out.println("bye");
      }
    }
 else {
      return a + b + c;
    }
  }
 else {
    if (a + b - c == 10) {
      System.out.println("hehe");
    }
 else     if (a > b) {
      System.out.println("hoo");
    }
  }
  return 5;
}

STEP 3: Sinh dữ liệu ngẫu nhiên cho các parameter [int, int, int]: [8, 8, 8]
STEP 4: Chạy dữ liệu ngẫu nhiên đấy, lưu những câu lệnh đã được chạy qua: [1 a > 10 false, 13 a + b - c == 10 false, 15 a > b false, 19 return 5;
 ]
STEP 5: Tìm node chưa được phủ: CFGNode{, content='System.out.println("hoo");
', isRootNode=false, isEndNode=false}
STEP 6: Sinh đường thi hành từ node chưa được phủ đấy
STEP 7: Thực thi tượng trưng đường thi hành và sinh test data tương ứng: [1, 0, 0]
STEP 8: Đánh dấu những câu lệnh (node) đã chạy qua sau khi thực hiện chạy hàm với dữ liệu vừa được sinh: [1 a > 10 false, 13 a + b - c == 10 false, 15 a > b true, 16 System.out.println("hoo");
 , 19 return 5;
 ]
STEP 9: Tìm node chưa được phủ: CFGNode{, content='System.out.println("hehe");
', isRootNode=false, isEndNode=false}
STEP 10: Sinh đường thi hành từ node chưa được phủ đấy
STEP 11: Thực thi tượng trưng đường thi hành và sinh test data tương ứng: [10, 0, 0]
STEP 12: Đánh dấu những câu lệnh (node) đã chạy qua sau khi thực hiện chạy hàm với dữ liệu vừa được sinh: [1 a > 10 false, 13 a + b - c == 10 true, 14 System.out.println("hehe");
 , 19 return 5;
 ]
STEP 13: Tìm node chưa được phủ: CFGNode{, content='c < 20', isRootNode=false, isEndNode=false}
STEP 14: Sinh đường thi hành từ node chưa được phủ đấy
STEP 15: Thực thi tượng trưng đường thi hành và sinh test data tương ứng: [11, 1, 19]
STEP 16: Đánh dấu những câu lệnh (node) đã chạy qua sau khi thực hiện chạy hàm với dữ liệu vừa được sinh: [1 a > 10 true, 2 c < 20 true, 3 int i=0 , 3 i < b true, 4 System.out.println("hello");
 , 3 i=i + 1 , 3 i < b false, 6 b < -5 false, 19 return 5;
 ]
STEP 17: Tìm node chưa được phủ: CFGNode{, content='return a + b + c;
', isRootNode=false, isEndNode=false}
STEP 18: Sinh đường thi hành từ node chưa được phủ đấy
STEP 19: Thực thi tượng trưng đường thi hành và sinh test data tương ứng: [11, 1, 20]
STEP 20: Đánh dấu những câu lệnh (node) đã chạy qua sau khi thực hiện chạy hàm với dữ liệu vừa được sinh: [1 a > 10 true, 2 c < 20 false, 10 return a + b + c;
 ]
STEP 21: Tìm node chưa được phủ: CFGNode{, content='System.out.println("bye");
', isRootNode=false, isEndNode=false}
STEP 22: Sinh đường thi hành từ node chưa được phủ đấy
STEP 23Kết thúc việc kiểm thử
